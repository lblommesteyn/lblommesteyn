"""gridconstraint: research prototype for predicting which transmission facilities a
proposed generator / battery / large load will constrain, using only information that
was public before the ISO study was published.

See README.md for the honesty statement: the benchmark shipped here runs on a
physics-grounded *simulated ISO* over a real public PJM-footprint nodal topology,
because the real study PDFs were unreachable from the build environment.
"""
__version__ = "0.1.0"
